# KSeF Invoice Monitor v0.3 - Environment Variables
# Use this file to store sensitive credentials securely
# Copy to .env and set chmod 600 .env for security

# === KSeF API ===
KSEF_TOKEN=your-ksef-authorization-token-here

# === Pushover (Mobile Notifications) ===
PUSHOVER_USER_KEY=your-pushover-user-key-here
PUSHOVER_API_TOKEN=your-pushover-app-token-here

# === Discord (Webhook Notifications) ===
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...

# === Slack (Webhook Notifications) ===
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/...

# === Email (SMTP Notifications) ===
EMAIL_PASSWORD=your-smtp-password-or-app-password

# === Webhook (Custom HTTP Endpoint) ===
WEBHOOK_TOKEN=your-webhook-auth-token

# Security Notes:
# - Never commit .env file to version control
# - Use chmod 600 .env to restrict file permissions
# - For production, use Docker secrets instead of .env
# - Rotate tokens regularly
